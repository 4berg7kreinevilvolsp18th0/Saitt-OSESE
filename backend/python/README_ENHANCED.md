# OSS DVFU Backend API - Enhanced Version 2.0

Расширенный FastAPI бэкенд для сайта ОСС ДВФУ с новыми возможностями.

## Новые возможности версии 2.0

### 🔍 Полнотекстовый поиск
- Поиск по обращениям (заголовок, описание, категория, институт)
- Поиск по контенту (заголовок, тело)
- Поиск по тегам обращений

### 📊 Расширенная аналитика
- Детальная статистика с временными периодами
- Статистика производительности пользователей
- Аналитика контента
- Тренды по дням (последние 30 дней)
- Среднее время ответа и решения

### 📥 Экспорт данных
- Экспорт обращений в CSV
- Экспорт обращений в Excel (с форматированием)
- Экспорт статистики в CSV
- Опция включения внутренних данных

### ⚡ Rate Limiting
- Ограничение запросов по эндпоинтам
- Защита от злоупотреблений
- Настраиваемые лимиты

### 📝 Логирование
- Автоматическое логирование всех запросов
- Время обработки запросов
- Обработка ошибок

### 🔄 Улучшенная сортировка
- Сортировка по различным полям
- Направление сортировки (asc/desc)
- Поддержка сортировки по статусу, приоритету, дедлайну

## Установка

### Новые зависимости

```bash
pip install -r requirements.txt
```

Новые зависимости:
- `openpyxl` - для экспорта в Excel
- `slowapi` - для rate limiting
- `redis` - для кэширования (опционально)

## Новые API Endpoints

### Поиск

#### Поиск обращений
```
GET /api/search/appeals?q=запрос&direction_id=uuid&status=new
```

#### Поиск контента
```
GET /api/search/content?q=запрос&type=news&published_only=true
```

#### Поиск по тегам
```
GET /api/search/appeals/tags?tags=тег1&tags=тег2
```

### Аналитика

#### Детальная статистика
```
GET /api/appeals/stats/detailed?start_date=2024-01-01&end_date=2024-12-31&direction_id=uuid
```

#### Производительность пользователя
```
GET /api/users/{user_id}/performance?start_date=2024-01-01&end_date=2024-12-31
```

#### Аналитика контента
```
GET /api/analytics/content?start_date=2024-01-01&end_date=2024-12-31
```

### Экспорт

#### Экспорт обращений в CSV
```
GET /api/export/appeals/csv?direction_id=uuid&status=new&include_internal=true
```

#### Экспорт обращений в Excel
```
GET /api/export/appeals/excel?direction_id=uuid&status=new&include_internal=true
```

#### Экспорт статистики
```
GET /api/export/stats/csv
```

## Rate Limiting

Лимиты по умолчанию:
- Поиск: 60 запросов/минуту
- Статистика: 20-30 запросов/минуту
- Экспорт: 10 запросов/минуту
- Обычные запросы: 100 запросов/минуту

При превышении лимита возвращается ошибка 429 Too Many Requests.

## Примеры использования

### Поиск обращений

```python
import requests

# Поиск по тексту
response = requests.get(
    "http://localhost:8000/api/search/appeals",
    params={"q": "ремонт", "status": "new"}
)
appeals = response.json()
```

### Экспорт в Excel

```python
import requests

response = requests.get(
    "http://localhost:8000/api/export/appeals/excel",
    params={"include_internal": True}
)

with open("appeals.xlsx", "wb") as f:
    f.write(response.content)
```

### Детальная статистика

```python
import requests
from datetime import date

response = requests.get(
    "http://localhost:8000/api/appeals/stats/detailed",
    params={
        "start_date": "2024-01-01",
        "end_date": "2024-12-31"
    }
)
stats = response.json()

print(f"Всего обращений: {stats['total']}")
print(f"Среднее время ответа: {stats['avg_response_time_hours']} часов")
```

## Структура новых модулей

```
backend/python/
├── search.py          # Полнотекстовый поиск
├── export.py          # Экспорт данных
├── analytics.py       # Расширенная аналитика
├── middleware.py      # Rate limiting и логирование
└── main.py            # Обновлённые эндпоинты
```

## Производительность

- Используйте кэширование для часто запрашиваемых данных
- Для больших экспортов используйте фоновые задачи
- Настройте индексы в БД для полей поиска

## Безопасность

- Все административные эндпоинты должны использовать аутентификацию
- Rate limiting защищает от DDoS
- Валидация всех входных данных через Pydantic

## Миграция с версии 1.0

Все существующие эндпоинты остаются совместимыми. Новые функции добавлены как дополнительные эндпоинты.

## Дальнейшее развитие

Планируется:
- Redis кэширование
- Webhooks для интеграций
- Фоновые задачи (Celery)
- Расширенная валидация
- Тестирование

