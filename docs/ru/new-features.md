# Новые функции и улучшения

## ✅ Реализовано

### 1. Поиск по обращениям
- ✅ Компонент `SearchBar` для поиска
- ✅ Поиск по заголовку, описанию и контакту
- ✅ Интеграция в админ-панель обращений
- ✅ API endpoint `/api/appeals/search` для полнотекстового поиска

### 2. Email уведомления
- ✅ API endpoint `/api/notifications/email`
- ✅ Автоматическая отправка при изменении статуса
- ✅ Поддержка Resend (можно добавить другие сервисы)
- ✅ Красивые HTML шаблоны писем

**Настройка:**
1. Зарегистрируйтесь на [Resend](https://resend.com) или другом email сервисе
2. Добавьте в Vercel:
   - `EMAIL_SERVICE=resend`
   - `EMAIL_API_KEY=your-api-key`
   - `EMAIL_FROM=noreply@oss-dvfu.ru`

### 3. История изменений обращений (Audit Trail)
- ✅ Таблица `appeal_history` в базе данных
- ✅ Автоматическое логирование изменений через триггеры
- ✅ Компонент `AppealHistory` для отображения истории
- ✅ API endpoint `/api/appeals/[id]/history`

**Что логируется:**
- Изменение статуса
- Изменение приоритета
- Назначение ответственного
- Установка дедлайна
- Добавление комментариев

### 4. PWA поддержка (Progressive Web App)
- ✅ Файл `manifest.ts` для PWA
- ✅ Возможность установить сайт как приложение
- ✅ Офлайн режим (базовая поддержка)

**Для полной поддержки PWA:**
1. Добавьте иконки в `public/`:
   - `icon-192.png` (192x192)
   - `icon-512.png` (512x512)
2. Настройте Service Worker для офлайн режима

## 🔄 В процессе

### 5. Публичные комментарии для студентов
- ⏳ Комментарии, видимые студентам по токену
- ⏳ Уведомления о новых комментариях

### 7. Календарь событий
- ⏳ Отображение событий и дедлайнов
- ⏳ Интеграция с обращениями

## 📋 Планируется

### 8. Рейтинг и отзывы
- Оценка работы ОСС
- Отзывы о решённых обращениях

### 9. Интеграция с VK
- Отображение постов из VK группы
- Автоматическая публикация новостей

### 10. Улучшенная аналитика
- Графики по времени
- Прогнозирование нагрузки
- SLA метрики

## Инструкции по настройке

### Email уведомления (Resend)

1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. Создайте API ключ
3. Добавьте в Vercel:
   ```
   EMAIL_SERVICE=resend
   EMAIL_API_KEY=re_xxxxxxxxxxxxx
   EMAIL_FROM=noreply@yourdomain.com
   ```
4. Подтвердите домен в Resend (для production)

### История изменений

1. Выполните `database/schema_audit.sql` в Supabase SQL Editor
2. История будет автоматически создаваться при изменениях
3. Просмотр истории доступен в админ-панели на карточке обращения

### PWA

1. Создайте иконки:
   - 192x192 PNG
   - 512x512 PNG
2. Разместите в `frontend/nextjs/public/`
3. Обновите пути в `manifest.ts` если нужно

## Использование

### Поиск по обращениям

В админ-панели `/admin/appeals`:
1. Используйте поисковую строку
2. Поиск работает в реальном времени
3. Фильтрует по заголовку, описанию и контакту

### История изменений

1. Откройте обращение в админ-панели
2. Нажмите "История" на карточке
3. Просмотрите все изменения с датами и авторами

### Переключение языка

1. Нажмите на переключатель языков в Header
2. Выберите: Рус / Eng / 中文
3. Язык сохраняется автоматически

