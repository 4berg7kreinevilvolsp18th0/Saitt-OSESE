# Руководство по миграции на защищенную систему входа

## 🎯 Что изменилось

### 1. Публичный вход и регистрация

**Новые пути:**
- `/login` - вход в систему (публичный)
- `/register` - регистрация (публичный)
- `/manage` - скрытая админ-панель (вместо `/admin`)

**Старые пути:**
- `/admin/*` - автоматически редиректятся на `/manage/*`
- `/admin/login` - редирект на `/login`

### 2. Защита от брутфорса

- Блокировка после 5 неудачных попыток
- Блокировка на 15 минут
- Rate limiting: 5 попыток входа в минуту

### 3. Логирование безопасности

- Все попытки входа логируются
- Таблица `security_log` в БД
- Мониторинг подозрительной активности

## 📋 Шаги миграции

### Шаг 1: Применить миграцию БД

1. Зайдите в Supabase Dashboard
2. Перейдите в SQL Editor
3. Выполните файл: `database/migrations/add_security_logging.sql`
4. Проверьте, что таблица `security_log` создана

### Шаг 2: Обновить ссылки (опционально)

Если у вас есть закладки или документы со ссылками на `/admin`:
- Они автоматически редиректятся на `/manage`
- Но лучше обновить на новые пути

### Шаг 3: Протестировать

1. Попробуйте войти на `/login`
2. Попробуйте зарегистрироваться на `/register`
3. Проверьте, что `/admin` редиректит на `/manage`
4. Попробуйте 5 раз войти с неверным паролем
5. Проверьте блокировку

### Шаг 4: Проверить логи

```sql
-- В Supabase SQL Editor
SELECT * FROM security_log 
ORDER BY created_at DESC 
LIMIT 20;
```

## 🔐 Безопасность

### Для пользователей:

1. **Используйте сильные пароли:**
   - Минимум 8 символов
   - Буквы, цифры, специальные символы
   - Не используйте простые пароли

2. **Не делитесь учетными данными:**
   - Каждый должен иметь свой аккаунт
   - Не передавайте пароли по незащищенным каналам

3. **При блокировке:**
   - Подождите 15 минут
   - Или обратитесь к администратору

### Для администраторов:

1. **Мониторинг:**
   - Регулярно проверяйте логи безопасности
   - Отслеживайте подозрительную активность
   - Блокируйте подозрительные IP

2. **Блокировка IP:**
   ```typescript
   // В middleware.ts
   blockedIPs.add('suspicious-ip');
   ```

3. **Проверка логов:**
   ```sql
   -- Топ IP по попыткам
   SELECT ip_address, COUNT(*) as attempts
   FROM security_log
   WHERE event_type = 'login_attempt'
   GROUP BY ip_address
   ORDER BY attempts DESC;
   ```

## ⚠️ Важно

1. **Старые пути работают:**
   - `/admin` → `/manage` (автоматический редирект)
   - Но лучше использовать новые пути

2. **Блокировка:**
   - Хранится в localStorage браузера
   - При очистке кэша блокировка снимается
   - В production можно использовать Redis для серверной блокировки

3. **Логирование:**
   - Email частично скрыт (первые 3 символа + ***)
   - IP адреса логируются полностью
   - Логи хранятся 90 дней (автоматическая очистка)

## 🚀 Дополнительные меры

### Рекомендуется:

1. **Настроить Cloudflare:**
   - WAF правила
   - DDoS защита
   - Bot Fight Mode

2. **Включить 2FA:**
   - Для администраторов
   - TOTP через приложение

3. **IP Whitelist:**
   - Если известны IP адреса ОСС
   - Ограничить доступ только с них

4. **Мониторинг:**
   - Настроить алерты
   - Email/Telegram уведомления при атаках

---

**Итог:** Система защищена и готова к использованию. Все старые ссылки продолжают работать через редиректы.

