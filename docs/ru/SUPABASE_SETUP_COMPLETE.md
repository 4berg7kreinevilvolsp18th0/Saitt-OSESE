# Полная инструкция по настройке Supabase

## 📋 Содержание

1. [Создание проекта Supabase](#1-создание-проекта-supabase)
2. [Настройка базы данных](#2-настройка-базы-данных)
3. [Настройка Storage](#3-настройка-storage)
4. [Настройка переменных окружения](#4-настройка-переменных-окружения)
5. [Проверка работоспособности](#5-проверка-работоспособности)
6. [Настройка GitHub Actions](#6-настройка-github-actions)

---

## 1. Создание проекта Supabase

### Шаг 1.1: Регистрация

1. Перейдите на https://supabase.com
2. Нажмите **"Start your project"** или **"Sign in"**
3. Войдите через GitHub (рекомендуется) или создайте аккаунт

### Шаг 1.2: Создание проекта

1. Нажмите **"New Project"**
2. Заполните форму:
   - **Name:** `oss-dvfu` (или любое другое имя)
   - **Database Password:** 
     - Придумайте надежный пароль (минимум 12 символов)
     - **ВАЖНО:** Сохраните пароль! Он понадобится для прямого доступа к БД
     - Пример: `MySecurePass123!@#`
   - **Region:** Выберите ближайший регион
     - Для России: `West Europe (London)` или `Central EU (Frankfurt)`
3. Нажмите **"Create new project"**
4. Дождитесь создания проекта (2-3 минуты)

---

## 2. Настройка базы данных

### Шаг 2.1: Получение ключей API

1. В Supabase Dashboard перейдите в **Settings** → **API**
2. Найдите секцию **Project URL**:
   - Скопируйте URL (выглядит как `https://xxxxx.supabase.co`)
   - Это ваш `NEXT_PUBLIC_SUPABASE_URL`
3. Найдите секцию **Project API keys**:
   - Скопируйте ключ из колонки **"anon public"** (длинная строка, начинается с `eyJ...`)
   - **НЕ копируйте "service_role" ключ!** Он секретный и не должен использоваться в браузере
   - Это ваш `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### Шаг 2.2: Выполнение миграций

**ВАЖНО:** Выполняйте миграции в указанном порядке!

#### 2.2.1: Основная схема

**⚠️ ВАЖНО: Это нужно выполнить ПЕРВЫМ!**

**Вариант A: Полная схема (рекомендуется)**

1. Перейдите в **SQL Editor** в Supabase Dashboard
2. Нажмите **"New query"**
3. Откройте файл `database/schema.sql` из проекта
4. Скопируйте весь код
5. Вставьте в SQL Editor
6. Нажмите **"Run"** (или Ctrl+Enter)
7. Дождитесь сообщения **"Success. No rows returned"** - это нормально!

**Вариант B: Минимальная схема (если возникли ошибки)**

Если при выполнении `schema.sql` возникли ошибки, используйте упрощенную версию:

1. Выполните `database/migrations/ensure_all_tables_exist.sql`
2. Это создаст только основные таблицы без RLS и других сложных элементов
3. Затем выполните остальные миграции

**Проверка:**
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```
Должны быть таблицы: `appeals`, `appeal_attachments`, `content`, и другие...

#### 2.2.2: Проверка таблиц (рекомендуется)

1. В SQL Editor создайте новый запрос
2. Откройте файл `database/migrations/check_tables_before_migration.sql`
3. Скопируйте и выполните код
4. Если видите ошибку - сначала выполните `schema.sql`
5. Если видите "Все необходимые таблицы существуют" - продолжайте

#### 2.2.3: Миграция файлов (автоудаление)

1. В SQL Editor создайте новый запрос
2. Откройте файл `database/migrations/add_file_retention.sql`
3. Скопируйте и выполните код

#### 2.2.4: Миграция анонимности

1. Создайте новый запрос
2. Откройте файл `database/migrations/improve_anonymity.sql`
3. Скопируйте и выполните код

#### 2.2.5: Миграция защиты контента

1. Создайте новый запрос
2. Откройте файл `database/migrations/add_content_protection.sql`
3. Скопируйте и выполните код

#### 2.2.6: Исправление безопасности

1. Создайте новый запрос
2. Откройте файл `database/migrations/fix_security_issues.sql`
3. Скопируйте и выполните код

#### 2.2.7: Исправление ошибок типов UUID

1. Создайте новый запрос
2. Откройте файл `database/migrations/fix_uuid_type_errors.sql`
3. Скопируйте и выполните код
4. **Это исправляет ошибки "operator does not exist: uuid = text"**

#### 2.2.6: (Опционально) Тестовые данные

1. Создайте новый запрос
2. Откройте файл `database/seed.sql`
3. Скопируйте и выполните код
4. Это добавит тестовые данные для проверки

### Шаг 2.3: Проверка таблиц

Выполните в SQL Editor:

```sql
-- Проверить, что все таблицы созданы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

Должны быть таблицы:
- `appeals`
- `appeal_attachments`
- `appeal_comments`
- `content`
- `content_audit_log`
- `directions`
- `documents`
- `student_organizations`
- `user_roles`
- и другие...

### Шаг 2.4: Настройка аутентификации

1. Перейдите в **Authentication** → **Providers**
2. Убедитесь, что **Email** включен (по умолчанию включен)
3. (Опционально) Включите другие провайдеры:
   - Google
   - GitHub
   - и т.д.

---

## 3. Настройка Storage

### Шаг 3.1: Создание bucket для файлов обращений

1. Перейдите в **Storage** в Supabase Dashboard
2. Нажмите **"New bucket"**
3. Заполните форму:
   - **Name:** `appeal-attachments`
   - **Public bucket:** ✅ **Включите** (чтобы файлы были доступны по публичным URL)
   - **File size limit:** `10485760` (10 MB в байтах)
   - **Allowed MIME types:** (оставьте пустым для всех типов)
4. Нажмите **"Create bucket"**

### Шаг 3.2: Настройка политик доступа для Storage

1. Перейдите в **Storage** → **Policies**
2. Выберите bucket `appeal-attachments`
3. Нажмите **"New Policy"**

#### Политика для загрузки (INSERT):

- **Policy name:** `Allow authenticated uploads`
- **Allowed operation:** `INSERT`
- **Policy definition:**
```sql
(bucket_id = 'appeal-attachments')
```
- **Check expression:** (оставьте пустым)

#### Политика для чтения (SELECT):

- **Policy name:** `Allow public reads`
- **Allowed operation:** `SELECT`
- **Policy definition:**
```sql
(bucket_id = 'appeal-attachments')
```

#### Политика для удаления (DELETE):

- **Policy name:** `Allow system deletes`
- **Allowed operation:** `DELETE`
- **Policy definition:**
```sql
(bucket_id = 'appeal-attachments')
```

---

## 4. Настройка переменных окружения

### Шаг 4.1: Локальная разработка

1. В папке `frontend/nextjs/` создайте файл `.env.local`
2. Добавьте следующие переменные:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Сайт
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**Замените:**
- `https://xxxxx.supabase.co` на ваш Project URL
- `eyJ...` на ваш anon public key

### Шаг 4.2: Production (Vercel)

1. Перейдите в ваш проект на Vercel
2. Откройте **Settings** → **Environment Variables**
3. Добавьте переменные для **Production**:

| Name | Value |
|------|-------|
| `NEXT_PUBLIC_SUPABASE_URL` | `https://xxxxx.supabase.co` |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJ...` |
| `NEXT_PUBLIC_SITE_URL` | `https://your-domain.com` |

4. Нажмите **"Save"**
5. Перезапустите деплой (Redeploy)

---

## 5. Проверка работоспособности

### Шаг 5.1: Проверка подключения

Выполните в SQL Editor:

```sql
-- Проверить подключение
SELECT version();

-- Проверить таблицы
SELECT COUNT(*) FROM directions;

-- Проверить RLS
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public';
```

### Шаг 5.2: Проверка Storage

1. Перейдите в **Storage** → **appeal-attachments**
2. Попробуйте загрузить тестовый файл
3. Проверьте, что файл появился

### Шаг 5.3: Проверка линтера

1. В Supabase Dashboard перейдите в **Database** → **Linter**
2. Запустите проверку
3. Убедитесь, что нет критических ошибок (ERROR)
4. Предупреждения (WARN) и информационные (INFO) можно оставить

### Шаг 5.4: Тестирование локально

1. Откройте терминал в папке `frontend/nextjs/`
2. Выполните:
```bash
npm install
npm run dev
```
3. Откройте http://localhost:3000
4. Проверьте, что сайт загружается без ошибок

---

## 6. Настройка GitHub Actions

### Шаг 6.1: Создание GitHub Actions workflow

1. В корне проекта создайте папку `.github/workflows/` (если её нет)
2. Создайте файл `.github/workflows/supabase-sync.yml`

### Шаг 6.2: Настройка workflow

Скопируйте следующий код в файл:

```yaml
name: Supabase Database Sync

on:
  push:
    branches:
      - main
    paths:
      - 'database/**'
      - '.github/workflows/supabase-sync.yml'
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  sync-database:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install Supabase CLI
        run: |
          npm install -g supabase
      
      - name: Link to Supabase project
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
        run: |
          supabase link --project-ref $SUPABASE_PROJECT_ID
      
      - name: Run migrations
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          supabase db push
```

### Шаг 6.3: Настройка GitHub Secrets

1. В вашем GitHub репозитории перейдите в **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **"New repository secret"**
3. Добавьте следующие секреты:

#### SUPABASE_ACCESS_TOKEN

1. В Supabase Dashboard перейдите в **Settings** → **Access Tokens**
2. Нажмите **"Generate new token"**
3. Дайте имя токену (например, "GitHub Actions")
4. Скопируйте токен
5. В GitHub добавьте secret:
   - **Name:** `SUPABASE_ACCESS_TOKEN`
   - **Value:** (вставьте токен)

#### SUPABASE_PROJECT_ID

1. В Supabase Dashboard перейдите в **Settings** → **General**
2. Найдите **Reference ID** (выглядит как `abcdefghijklmnop`)
3. Скопируйте его
4. В GitHub добавьте secret:
   - **Name:** `SUPABASE_PROJECT_ID`
   - **Value:** (вставьте Reference ID)

#### SUPABASE_DB_PASSWORD

1. Используйте пароль, который вы создали при создании проекта
2. В GitHub добавьте secret:
   - **Name:** `SUPABASE_DB_PASSWORD`
   - **Value:** (вставьте пароль БД)

### Шаг 6.4: Альтернатива: Ручной коммит через Supabase CLI

Если не хотите использовать GitHub Actions, можно настроить локальный скрипт:

**⚠️ ВАЖНО:** Supabase CLI нельзя устанавливать через `npm install -g`!

#### Установка Supabase CLI на Windows:

**Вариант A: Через Scoop (рекомендуется)**
```powershell
# Установите Scoop (если еще не установлен)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression

# Установите Supabase CLI
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase
```

**Вариант B: Через Chocolatey**
```powershell
# Установите Chocolatey (если еще не установлен)
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# Установите Supabase CLI
choco install supabase
```

**Вариант C: Ручная установка**
1. Скачайте с https://github.com/supabase/cli/releases
2. Распакуйте и добавьте в PATH

#### Использование:

1. Войдите в Supabase:
```powershell
supabase login
```

2. Свяжите проект:
```powershell
supabase link --project-ref your-project-id
```

3. Примените миграции:
```powershell
supabase db push
```

---

## 7. Создание первого пользователя

### Шаг 7.1: Создание пользователя через Supabase

1. Перейдите в **Authentication** → **Users**
2. Нажмите **"Add user"** → **"Create new user"**
3. Заполните:
   - **Email:** `admin@example.com`
   - **Password:** (придумайте надежный пароль)
   - **Auto Confirm User:** ✅ Включите
4. Нажмите **"Create user"**
5. Скопируйте **User UID** (выглядит как UUID)

### Шаг 7.2: Назначение роли

Выполните в SQL Editor:

```sql
-- Добавить роль board (руководство ОСС)
INSERT INTO user_roles (user_id, role, direction_id)
VALUES (
  'your-user-uuid-here', -- Замените на User UID
  'board',
  NULL
);
```

### Шаг 7.3: Проверка доступа

1. Войдите на сайт с созданным пользователем
2. Перейдите в `/admin`
3. Убедитесь, что видите админ-панель

---

## 8. Чек-лист настройки

### База данных
- [ ] Проект создан в Supabase
- [ ] Ключи API скопированы
- [ ] Выполнен `schema.sql`
- [ ] Выполнены все миграции
- [ ] Таблицы созданы и проверены
- [ ] RLS политики работают

### Storage
- [ ] Bucket `appeal-attachments` создан
- [ ] Политики доступа настроены
- [ ] Тестовый файл загружен

### Переменные окружения
- [ ] `.env.local` создан локально
- [ ] Переменные добавлены в Vercel
- [ ] Сайт работает без ошибок

### GitHub Actions
- [ ] Workflow файл создан
- [ ] Secrets добавлены в GitHub
- [ ] Тестовый коммит выполнен

### Пользователи
- [ ] Первый пользователь создан
- [ ] Роль назначена
- [ ] Доступ проверен

---

## 9. Решение проблем

### Проблема: "Supabase не настроен"

**Решение:**
1. Проверьте, что `.env.local` существует
2. Проверьте, что переменные заполнены правильно
3. Перезапустите dev сервер (`npm run dev`)

### Проблема: "404 NOT_FOUND"

**Решение:**
1. Проверьте, что Project URL правильный
2. Проверьте, что используете **anon** ключ, а не **service_role**
3. Убедитесь, что проект активен в Supabase

### Проблема: "Permission denied"

**Решение:**
1. Проверьте RLS политики
2. Убедитесь, что пользователь авторизован
3. Проверьте роли пользователя в таблице `user_roles`

### Проблема: "Bucket not found"

**Решение:**
1. Проверьте, что bucket создан
2. Проверьте имя bucket (должно быть `appeal-attachments`)
3. Проверьте политики доступа

---

## 10. Полезные ссылки

- [Supabase Dashboard](https://app.supabase.com)
- [Supabase Documentation](https://supabase.com/docs)
- [Supabase CLI](https://supabase.com/docs/guides/cli)
- [RLS Policies](https://supabase.com/docs/guides/database/postgres/row-level-security)

---

## Готово! 🎉

После выполнения всех шагов ваш проект должен быть полностью настроен и готов к работе.

Если возникнут вопросы, проверьте:
- Логи в Supabase Dashboard → Logs
- Консоль браузера (F12)
- Логи Vercel (если используете)

