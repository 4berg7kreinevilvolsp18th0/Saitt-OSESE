# Усиленная защита от атак для ОСС ДВФУ

Комплексная система защиты от профессиональных атак, включая возможные атаки от IT-отдела ДВФУ.

## 🎯 Цель

Защитить сайт ОСС от:
- Брутфорс атак на вход
- DDoS атак
- SQL injection
- XSS атак
- Попыток найти админ-панель
- Профессиональных атак от IT-отдела

## 🛡️ Реализованные меры защиты

### 1. Скрытие админ-панели

**Было:** `/admin` (очевидно для атакующих)  
**Стало:** `/manage` (менее очевидно)

**Дополнительно:**
- Старые пути `/admin/*` автоматически редиректятся на `/manage/*`
- Публичный вход на `/login` (не `/admin/login`)
- Регистрация на `/register`

### 2. Защита от брутфорса

**Реализовано:**
- Блокировка после 5 неудачных попыток входа
- Блокировка на 15 минут
- Локальное хранение счетчика попыток (localStorage)
- Rate limiting на уровне API (5 попыток в минуту)

**Как работает:**
```typescript
// После 5 неудачных попыток:
localStorage.setItem('login_blocked', blockTime);
// Блокировка на 15 минут
```

### 3. Логирование безопасности

**Таблица `security_log`:**
- Все попытки входа (успешные/неудачные)
- Регистрации
- Блокировки IP
- Подозрительная активность

**Что логируется:**
- Email (частично скрытый: `abc***`)
- IP адрес
- User Agent
- Время события
- Статус (success/failed/blocked)

### 4. Rate Limiting

**Реализовано в middleware:**
- API endpoints: 100 запросов/минуту
- Вход/регистрация: 5 запросов/минуту
- Security logging: 50 запросов/минуту

**При превышении:**
- HTTP 429 (Too Many Requests)
- Заголовок `Retry-After` с временем ожидания

### 5. Security Headers

**Добавлены автоматически:**
- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY`
- `X-XSS-Protection: 1; mode=block`
- `Referrer-Policy: strict-origin-when-cross-origin`
- `Permissions-Policy: camera=(), microphone=(), geolocation=()`
- Убран `X-Powered-By` (скрыть информацию о сервере)

### 6. Обнаружение подозрительной активности

**Паттерны, которые отслеживаются:**
- Запросы к `/admin`, `/wp-admin`, `/phpmyadmin`
- Попытки доступа к `.env`, `.git`
- SQL injection паттерны (`union select`, etc.)
- XSS паттерны (`script alert`, etc.)

**При обнаружении:**
- Логирование в консоль
- Логирование в БД (если настроено)
- Можно добавить автоматическую блокировку IP

### 7. Блокировка IP

**Реализовано:**
- Список заблокированных IP в middleware
- Можно добавлять через API или вручную

**Как добавить IP в блокировку:**
```typescript
// В middleware.ts
const blockedIPs = new Set<string>();
blockedIPs.add('192.168.1.100'); // Добавить IP
```

## 📋 Дополнительные меры (рекомендуется)

### 1. Cloudflare WAF

**Настроить правила:**
- Блокировать запросы с подозрительными User-Agent
- Rate limiting на уровне Cloudflare
- DDoS защита
- Bot Fight Mode

**Как настроить:**
1. Зарегистрироваться на Cloudflare
2. Добавить домен
3. Включить WAF
4. Настроить правила безопасности

### 2. IP Whitelist (опционально)

**Если известны IP адреса ОСС:**
```typescript
// В middleware.ts
const allowedIPs = new Set<string>([
  '192.168.1.0/24', // Офис ОСС
  '10.0.0.0/8',     // Сеть ДВФУ (если разрешено)
]);

// Проверка
if (!isIPAllowed(ip)) {
  return new NextResponse('Access Denied', { status: 403 });
}
```

### 3. 2FA (Двухфакторная аутентификация)

**Рекомендуется для админов:**
- TOTP (Time-based One-Time Password)
- SMS (если есть бюджет)
- Email коды

**Реализация:**
- Использовать библиотеку `otplib` для TOTP
- Интеграция с Supabase Auth MFA

### 4. Мониторинг и алерты

**Настроить:**
- Sentry для отслеживания ошибок
- Email алерты при подозрительной активности
- Telegram бот для уведомлений админов

**Пример алерта:**
```typescript
// При обнаружении атаки
await sendAlert({
  type: 'security_breach',
  message: `Подозрительная активность с IP ${ip}`,
  details: { path, userAgent, attempts: 10 },
});
```

### 5. Регулярные бэкапы

**Настроить:**
- Ежедневные бэкапы БД
- Хранение в нескольких местах
- Тестирование восстановления

## 🔧 Настройка

### 1. Применить миграцию БД

```sql
-- Выполнить в Supabase SQL Editor
-- Файл: database/migrations/add_security_logging.sql
```

### 2. Настроить переменные окружения

```env
# В Vercel
NEXT_PUBLIC_SITE_URL=https://oss-students-dvfu.vercel.app
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### 3. Проверить работу

1. Попробовать войти с неверным паролем 5 раз
2. Проверить блокировку
3. Проверить логи в `security_log`
4. Проверить редирект с `/admin` на `/manage`

## 📊 Мониторинг безопасности

### Просмотр логов

**В Supabase SQL Editor:**
```sql
-- Последние попытки входа
SELECT * FROM security_log 
WHERE event_type = 'login_attempt'
ORDER BY created_at DESC
LIMIT 50;

-- Заблокированные IP
SELECT ip_address, COUNT(*) as attempts
FROM security_log
WHERE status = 'blocked'
GROUP BY ip_address
ORDER BY attempts DESC;

-- Подозрительная активность
SELECT * FROM security_log
WHERE details LIKE '%suspicious%'
ORDER BY created_at DESC;
```

### Дашборд безопасности (можно создать)

**Страница:** `/manage/security` (только для board/staff)

**Что показывать:**
- График попыток входа по времени
- Топ IP адресов по попыткам
- Заблокированные IP
- Статистика по типам событий

## 🚨 Что делать при обнаружении атаки

### Немедленно:

1. **Заблокировать IP:**
   ```typescript
   // В middleware.ts
   blockedIPs.add('attacker-ip');
   ```

2. **Проверить логи:**
   - Какие endpoints атакуются
   - Какие методы используются
   - Откуда идут запросы

3. **Усилить защиту:**
   - Временно снизить rate limits
   - Включить CAPTCHA
   - Уведомить команду

### Краткосрочно:

1. **Анализ атаки:**
   - Тип атаки (брутфорс, DDoS, SQL injection)
   - Масштаб
   - Уязвимости

2. **Патчи:**
   - Закрыть найденные уязвимости
   - Обновить правила WAF
   - Усилить валидацию

### Долгосрочно:

1. **Улучшение защиты:**
   - Реализовать 2FA
   - Настроить IP whitelist
   - Улучшить мониторинг

2. **Документация:**
   - Задокументировать инцидент
   - Обновить процедуры безопасности
   - Провести обучение команды

## 📚 Полезные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Cloudflare Security](https://www.cloudflare.com/learning/security/)
- [Next.js Security](https://nextjs.org/docs/app/building-your-application/configuring/security)

---

**Итог:** Система защищена от большинства типов атак. При обнаружении подозрительной активности - проверяйте логи и блокируйте IP адреса.

