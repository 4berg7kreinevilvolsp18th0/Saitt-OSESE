# Работа с дизайном для ОСС ДВФУ

Привет! Если ты дизайнер и работаешь с этим проектом, этот документ поможет быстро разобраться, что к чему.

## Что это за проект

Сайт для Объединённого совета студентов ДВФУ. Студенты могут подавать обращения, смотреть новости и гайды, а члены ОСС — работать с обращениями в админ-панели.

## Технологии

- **Frontend**: Next.js 14 (React) + TypeScript
- **Стили**: Tailwind CSS
- **Шрифты**: SF UI Text и SF UI Display (Apple)
- **Анимации**: CSS animations
- **Темы**: Светлая и темная

## Структура проекта

```
frontend/nextjs/
├── app/                    # Страницы
│   ├── page.tsx           # Главная
│   ├── directions/        # Страницы направлений
│   ├── appeal/            # Форма обращения
│   ├── admin/             # Админ-панель
│   └── globals.css        # Глобальные стили
├── components/            # Компоненты
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── DirectionCard.tsx
│   └── ...
├── lib/                   # Утилиты
│   ├── theme.ts          # Цвета
│   └── directions.ts     # Данные направлений
└── tailwind.config.js    # Конфигурация Tailwind
```

## Цвета

### Основные
- **OSS Red**: `#D11F2A` (основной цвет)
- **OSS Dark**: `#0F1115` (фон темной темы)

### Направления
- **Правовой комитет**: `#1F2A44` (темно-синий), золотистый `#D4AF37`, темно-синий `#1A2332`
- **Инфраструктурный блок**: `rgb(13, 90, 206)` (синий)
- **Стипендиальный комитет**: `#2E8B57` (зеленый)
- **Иностранным студентам**: `#F5B301` (желтый)
- **Нейтральный/Другое**: `#6B7280` (серый)

### Светлая тема
- Фон: градиент от `#FFFFFF` → `#FAFBFC` → `#F5F7FA`
- Текст основной: `#1F2937`
- Текст вторичный: `#6B7280`
- Границы: `rgba(0, 0, 0, 0.08)`

## Шрифты

- **Заголовки**: SF UI Display
- **Текст**: SF UI Text

### Размеры заголовков
- `h1`: `clamp(2rem, 5vw, 3.5rem)`, вес 700
- `h2`: `clamp(1.5rem, 4vw, 2.5rem)`, вес 600
- `h3`: `clamp(1.25rem, 3vw, 2rem)`, вес 600

### Letter-spacing
- Заголовки: `-0.02em` до `-0.03em`
- Uppercase текст: `tracking-wider`

## Компоненты

### Карточки направлений
- Скругление: `rounded-xl sm:rounded-2xl`
- Отступы: `p-5 sm:p-7`
- Границы: `border-2` с градиентами
- Hover: подъем на 4px, увеличение тени

### Кнопки
- Primary: красный градиент с тенями
- Secondary: прозрачный фон с границей
- Скругление: `rounded-xl`
- Отступы: `px-6 sm:px-8 py-3 sm:py-3.5`

### Эффекты
- Тени: многослойные для глубины
- Backdrop blur: для стеклянных эффектов
- Градиенты: тонкие, элегантные

## Работа в Figma

### Организация файла

```
📁 ОСС ДВФУ Design System
  📁 01. Foundations
    📄 Colors
    📄 Typography
    📄 Spacing
    📄 Shadows
  📁 02. Components
    📄 Buttons
    📄 Cards
    📄 Forms
    📄 Navigation
  📁 03. Pages
    📄 Homepage (Dark)
    📄 Homepage (Light)
    📄 Directions
    📄 Appeal Form
  📁 04. Assets
    📄 Icons
    📄 Logos
```

### Важные настройки

**Auto Layout**
- Используй для всех компонентов
- Настрой правильные constraints
- Добавь responsive behavior

**Variants**
- Создай для тем (Dark/Light)
- Создай для состояний (Default/Hover/Active)
- Используй Component Properties для цветов направлений

**Styles**
- Text Styles для всех размеров заголовков
- Color Styles для палитры
- Effect Styles для теней

### Экспорт из Figma

**Плагины:**
- **Figma Tokens** — экспорт дизайн-токенов
- **Figma to Code** — генерация CSS/React кода
- **Figma to Tailwind** — конвертация в Tailwind классы

**Что экспортировать:**
1. Цвета — через Figma Tokens в JSON
2. Типографика — таблица с размерами и весами
3. Компоненты — через Figma Dev Mode (CSS)
4. Иконки — SVG, оптимизированные

## Процесс работы

1. **Изучи проект** — посмотри текущий сайт, почитай брендбук
2. **Создай дизайн-систему** — цвета, типографика, компоненты
3. **Спроектируй страницы** — макеты в Figma
4. **Добавь прототип** — интерактивный прототип для демонстрации
5. **Передай разработчику** — экспортируй токены, добавь комментарии

## Что нужно передать разработчику

### Дизайн-токены
```json
{
  "colors": {
    "oss-red": "#D11F2A",
    "oss-dark": "#0F1115"
  },
  "typography": {
    "h1": {
      "fontSize": "clamp(2rem, 5vw, 3.5rem)",
      "fontWeight": 700,
      "letterSpacing": "-0.03em"
    }
  }
}
```

### Компоненты
- Все состояния: Default, Hover, Active, Disabled, Focus
- Responsive версии для всех breakpoints (640px, 768px, 1024px, 1280px)
- Анимации: длительность, easing функции, задержки

## Полезные ссылки

- Система дизайна: `docs/ru/professional-design-system.md`
- Градиенты: `docs/ru/gradients-brandbook.md`
- Брендбук: `брендбук осс.pdf`

## Чеклист перед передачей

- [ ] Все цвета из палитры добавлены
- [ ] Типографика соответствует системе
- [ ] Компоненты с variants
- [ ] Responsive версии для всех breakpoints
- [ ] Темная и светлая тема
- [ ] Все состояния компонентов
- [ ] Комментарии в Figma
- [ ] Дизайн-токены экспортированы
- [ ] Интерактивный прототип готов

Удачи! Если что-то непонятно — спрашивай.
