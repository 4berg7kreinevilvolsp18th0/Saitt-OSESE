# Стратегия масштабирования Supabase для ОСС ДВФУ

Анализ реальных потребностей и план оптимизации для работы с 3,000-20,000 обращений в год при 20,000 студентов.

## 📊 Реальный расчет потребностей

### Размер данных на обращение

**Базовое обращение в БД:**
- `id` (UUID): 16 bytes
- `title` (текст): ~100-200 bytes
- `description` (текст): ~500-2000 bytes (среднее 1 KB)
- `status`, `priority`, `direction_id`: ~50 bytes
- `contact_type`, `contact_value`: ~100 bytes
- `created_at`, `updated_at`: ~20 bytes
- Метаданные (JSON): ~200 bytes
- **Итого: ~2-3 KB на обращение**

**Вложения (файлы):**
- Средний размер: 200-500 KB
- Процент обращений с вложениями: ~30%
- Хранятся в Supabase Storage (отдельно от БД)

### Расчет для разных сценариев

#### Сценарий 1: 3,000 обращений/год (минимальный)
- Текст в БД: 3,000 × 3 KB = **9 MB**
- Вложения: 3,000 × 0.3 × 300 KB = **270 MB**
- **Итого: ~280 MB** ✅ (в пределах 500 MB БД + 1 GB Storage)

#### Сценарий 2: 10,000 обращений/год (средний)
- Текст в БД: 10,000 × 3 KB = **30 MB**
- Вложения: 10,000 × 0.3 × 300 KB = **900 MB**
- **Итого: ~930 MB** ⚠️ (близко к лимиту Storage)

#### Сценарий 3: 20,000 обращений/год (максимальный)
- Текст в БД: 20,000 × 3 KB = **60 MB**
- Вложения: 20,000 × 0.3 × 300 KB = **1.8 GB**
- **Итого: ~1.86 GB** ❌ (превышает лимиты)

### Другие данные

**Пользователи (auth.users):**
- ~500-1000 активных пользователей (члены ОСС, админы)
- ~50 bytes на пользователя = **25-50 KB** ✅

**Контент (новости, гайды):**
- ~100-200 статей
- ~50-100 KB на статью = **5-20 MB** ✅

**Логи и метаданные:**
- Логи уведомлений: ~1 KB на запись
- ~10,000 записей/год = **10 MB** ✅

**Итого для всех данных:**
- Минимум: ~320 MB ✅
- Средний: ~970 MB ⚠️
- Максимум: ~1.9 GB ❌

---

## 🎯 Проблемы и решения

### Проблема 1: Превышение лимита Storage (1 GB)

**Решение 1: Архивация старых обращений**

Автоматически архивировать обращения старше 6-12 месяцев:
- Перемещать в отдельную таблицу `appeals_archive`
- Удалять вложения из Storage (или перемещать в дешевое хранилище)
- Оставлять только метаданные в основной БД

**Решение 2: Внешнее хранилище для файлов**

Использовать Cloudflare R2 (10 GB бесплатно) для вложений:
- Загружать файлы напрямую в R2
- Хранить только ссылки в Supabase
- Экономия: ~1.8 GB в Supabase Storage

**Решение 3: Сжатие файлов**

Автоматически сжимать изображения и PDF:
- Изображения: уменьшать до 1920px, оптимизировать JPEG
- PDF: сжимать через Ghostscript
- Экономия: ~50-70% размера файлов

**Решение 4: Ограничение размера вложений**

- Максимальный размер: 5 MB на файл
- Максимум 3 файла на обращение
- Экономия: предотвращает загрузку огромных файлов

### Проблема 2: Превышение лимита БД (500 MB)

**Решение 1: Очистка старых данных**

- Удалять логи старше 3 месяцев
- Архивировать старые обращения (см. выше)
- Очищать неиспользуемые сессии

**Решение 2: Оптимизация структуры**

- Использовать `TEXT` вместо `VARCHAR` для длинных полей
- Сжимать JSON метаданные
- Удалять неиспользуемые поля

**Решение 3: Партиционирование**

Разделить таблицу `appeals` по годам:
- `appeals_2024`
- `appeals_2025`
- Старые партиции можно архивировать

### Проблема 3: Трафик и нагрузка

**Лимиты Vercel:**
- 100 GB трафика/месяц
- Функции: 100 GB-hours/месяц

**Расчет трафика:**
- 20,000 студентов
- ~10% активных пользователей = 2,000
- ~5 страниц на пользователя/месяц
- ~500 KB на страницу
- **Итого: ~5 GB/месяц** ✅ (в пределах лимита)

**Решение: Кэширование**

- Кэшировать статические страницы (Next.js ISR)
- Кэшировать API ответы (Vercel Edge Cache)
- CDN для статики (Cloudflare)

### Проблема 4: Пиковые нагрузки

**Сценарий:** Все студенты заходят одновременно (начало семестра, важные новости)

**Решение 1: Rate Limiting**

Уже реализовано в backend:
- Ограничение запросов: 100 req/min на пользователя
- Защита от DDoS

**Решение 2: Очередь запросов**

Для создания обращений:
- Использовать очередь (Redis или in-memory)
- Обрабатывать по очереди, не блокируя пользователя

**Решение 3: Мониторинг**

- Настроить алерты при приближении к лимитам
- Мониторить использование через Supabase Dashboard
- Отслеживать трафик через Vercel Analytics

---

## 🚀 План оптимизации (по приоритету)

### Этап 1: Немедленно (до запуска)

1. **Ограничить размер вложений**
   - Максимум 5 MB на файл
   - Максимум 3 файла на обращение
   - Валидация на frontend и backend

2. **Настроить сжатие изображений**
   - Автоматическое сжатие при загрузке
   - Конвертация в WebP для браузеров

3. **Настроить мониторинг**
   - Дашборд использования Supabase
   - Алерты при 80% использования

### Этап 2: В первые 3 месяца

1. **Реализовать архивацию**
   - Автоматическая архивация обращений старше 6 месяцев
   - Миграция в `appeals_archive`
   - Удаление старых вложений

2. **Оптимизировать запросы**
   - Добавить индексы для частых запросов
   - Оптимизировать JOIN'ы
   - Использовать пагинацию везде

3. **Настроить кэширование**
   - ISR для статических страниц
   - Edge Cache для API
   - CDN через Cloudflare

### Этап 3: При росте (6-12 месяцев)

1. **Мигрировать файлы в Cloudflare R2**
   - 10 GB бесплатно
   - Без платы за трафик
   - Интеграция с Supabase

2. **Партиционирование таблиц**
   - Разделить по годам
   - Автоматическая ротация

3. **Запросить помощь у ДВФУ**
   - SMTP сервер для email
   - Возможно, выделенный сервер для архива

---

## 💾 Альтернативные решения

### Вариант 1: Остаться на Supabase (рекомендуется)

**Плюсы:**
- Уже настроено
- Простое управление
- Автоматические бэкапы

**Минусы:**
- Лимиты на бесплатном плане
- Нужна оптимизация

**Стоимость при росте:**
- Pro план: $25/месяц (8 GB БД, 100 GB Storage)
- Достаточно для 20,000 обращений/год

### Вариант 2: Гибридный подход

**Supabase + Cloudflare R2:**
- Supabase: БД и аутентификация
- Cloudflare R2: Файлы (10 GB бесплатно)
- Vercel: Хостинг

**Плюсы:**
- Максимальное использование бесплатных лимитов
- Масштабируемость

**Минусы:**
- Сложнее настройка
- Нужна интеграция R2 с Supabase

### Вариант 3: Self-hosted (если ДВФУ даст сервер)

**PostgreSQL на сервере ДВФУ:**
- Безлимитная БД
- Полный контроль
- Бесплатно (если сервер предоставлен)

**Плюсы:**
- Безлимитно
- Полный контроль

**Минусы:**
- Нужен сервер от ДВФУ
- Нужна поддержка IT-отдела
- Нужны бэкапы

---

## 📋 Чеклист оптимизации

### Немедленно (до запуска)

- [ ] Ограничить размер вложений (5 MB, 3 файла)
- [ ] Настроить сжатие изображений
- [ ] Добавить мониторинг использования
- [ ] Настроить алерты при 80% использования

### В первые 3 месяца

- [ ] Реализовать автоматическую архивацию
- [ ] Оптимизировать индексы БД
- [ ] Настроить кэширование (ISR, Edge Cache)
- [ ] Добавить CDN (Cloudflare)

### При росте (6-12 месяцев)

- [ ] Мигрировать файлы в Cloudflare R2
- [ ] Реализовать партиционирование
- [ ] Запросить помощь у ДВФУ (SMTP, сервер)
- [ ] Рассмотреть переход на Supabase Pro ($25/месяц)

---

## 🔍 Мониторинг и метрики

### Ключевые метрики для отслеживания

1. **Использование БД:**
   - Текущий размер: `SELECT pg_size_pretty(pg_database_size('postgres'));`
   - Размер таблиц: `SELECT pg_size_pretty(pg_total_relation_size('appeals'));`
   - Цель: < 400 MB (80% от 500 MB)

2. **Использование Storage:**
   - Через Supabase Dashboard → Storage
   - Цель: < 800 MB (80% от 1 GB)

3. **Трафик Vercel:**
   - Через Vercel Dashboard → Analytics
   - Цель: < 80 GB/месяц (80% от 100 GB)

4. **Количество обращений:**
   - Через админ-панель → Статистика
   - Отслеживать рост

### Алерты

Настроить уведомления при:
- Использование БД > 400 MB (80%)
- Использование Storage > 800 MB (80%)
- Трафик Vercel > 80 GB/месяц
- Количество обращений > 1,000/месяц

---

## 💡 Рекомендации для ОСС

### Краткосрочная стратегия (первые 6 месяцев)

1. **Начать с бесплатного плана Supabase**
   - Достаточно для 3,000-5,000 обращений/год
   - Реализовать оптимизации из Этапа 1 и 2

2. **Мониторить использование**
   - Еженедельно проверять метрики
   - Отслеживать рост обращений

3. **Планировать масштабирование**
   - При приближении к 80% лимитов — готовить миграцию

### Долгосрочная стратегия (год+)

1. **При росте до 10,000+ обращений/год:**
   - Мигрировать файлы в Cloudflare R2
   - Реализовать архивацию
   - Рассмотреть Supabase Pro ($25/месяц)

2. **Запросить помощь у ДВФУ:**
   - SMTP сервер для email (экономия на Resend)
   - Возможно, выделенный сервер для архива

3. **Оптимизировать процессы:**
   - Автоматизировать архивацию
   - Очищать неиспользуемые данные
   - Оптимизировать запросы

---

## 🎯 Итоговые выводы

### Для 3,000 обращений/год
✅ **Бесплатного плана Supabase достаточно**
- Нужны базовые оптимизации
- Мониторинг использования

### Для 10,000 обращений/год
⚠️ **Нужны оптимизации**
- Архивация старых данных
- Миграция файлов в R2
- Или переход на Supabase Pro

### Для 20,000 обращений/год
❌ **Нужен платный план или альтернатива**
- Supabase Pro ($25/месяц)
- Или гибрид: Supabase + Cloudflare R2
- Или self-hosted (если ДВФУ даст сервер)

### Рекомендация
**Начать с бесплатного плана + оптимизации:**
1. Ограничить размер вложений
2. Настроить архивацию
3. Мигрировать файлы в R2
4. Мониторить использование

**При росте:**
- Перейти на Supabase Pro ($25/месяц)
- Или запросить сервер у ДВФУ

**Итог:** С правильной оптимизацией бесплатного плана хватит на 6-12 месяцев. При росте можно масштабироваться плавно.

