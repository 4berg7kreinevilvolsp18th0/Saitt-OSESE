# Зимняя/Новогодняя тема

## 🎄 Описание

Зимняя тема автоматически активируется в зимние месяцы (декабрь, январь, февраль) и добавляет на сайт:
- ❄️ Анимированные снежинки
- 🎨 Зимнюю цветовую схему (синие и голубые оттенки)
- ✨ Специальные эффекты и градиенты
- 🎁 Новогоднее настроение

## 🎯 Автоматическая активация

Тема автоматически включается в:
- **Декабрь** (месяц 11)
- **Январь** (месяц 0)
- **Февраль** (месяц 1)

## 🔧 Ручное управление

### Включение/выключение

В правом нижнем углу экрана есть кнопка с иконкой:
- ❄️ - включить зимнюю тему
- ☀️ - выключить зимнюю тему

Настройка сохраняется в `localStorage` и будет применяться даже вне зимних месяцев.

## 🎨 Цветовая схема

### Темная тема (зима)
- **Основной фон:** Темно-синий градиент (#1E3A5F → #2C5282)
- **Акценты:** Голубой (#87CEEB), Светло-голубой (#E0F2FE)
- **Кнопки:** Синий градиент (#4A90E2 → #2C5282)

### Светлая тема (зима)
- **Основной фон:** Светло-голубой градиент (#E0F2FE → #87CEEB)
- **Акценты:** Темно-синий (#1E3A5F)
- **Карточки:** Полупрозрачный белый с голубыми границами

## ❄️ Снежинки

- **Количество:** 50 снежинок
- **Размер:** 4-10px
- **Скорость:** 3-7 секунд на падение
- **Прозрачность:** 0.3-1.0
- **Анимация:** Плавное падение с вращением

## 📁 Файлы

- `components/Snowflakes.tsx` - компонент снежинок
- `components/WinterTheme.tsx` - переключатель темы
- `components/ThemeProvider.tsx` - провайдер темы (обновлен)
- `app/globals.css` - зимние стили (секция "ЗИМНЯЯ/НОВОГОДНЯЯ ТЕМА")

## 🎨 CSS классы

### Основной класс
```css
.winter-theme
```
Добавляется к `document.documentElement` при активации темы.

### Зимние стили
- `.winter-theme .premium-card` - зимние карточки
- `.winter-theme .premium-button` - зимние кнопки
- `.winter-theme .text-oss-red` - зимние акценты
- `.winter-theme .gradient-accent` - зимние градиенты

## 🔄 Программное управление

### Проверка активности
```typescript
const isWinter = document.documentElement.classList.contains('winter-theme');
```

### Включение темы
```typescript
document.documentElement.classList.add('winter-theme');
localStorage.setItem('winter-theme', 'true');
window.dispatchEvent(new Event('winter-theme-change'));
```

### Выключение темы
```typescript
document.documentElement.classList.remove('winter-theme');
localStorage.setItem('winter-theme', 'false');
window.dispatchEvent(new Event('winter-theme-change'));
```

## 🎁 Особенности

1. **Автоматическая активация** - работает без настроек в зимние месяцы
2. **Сохранение настроек** - выбор пользователя сохраняется
3. **Плавные переходы** - все изменения анимированы
4. **Производительность** - снежинки оптимизированы (pointer-events: none)
5. **Адаптивность** - работает на всех устройствах

## 🐛 Отключение для определенных элементов

Если нужно отключить зимнюю тему для конкретного элемента:
```html
<div class="no-winter-theme">
  <!-- Этот элемент не будет затронут зимней темой -->
</div>
```

## 📝 Примеры использования

### В компоненте
```tsx
import { useEffect, useState } from 'react';

export default function MyComponent() {
  const [isWinter, setIsWinter] = useState(false);

  useEffect(() => {
    setIsWinter(document.documentElement.classList.contains('winter-theme'));
  }, []);

  return (
    <div className={isWinter ? 'winter-accent' : 'normal-accent'}>
      Контент
    </div>
  );
}
```

---

**С Новым годом! 🎉❄️**

