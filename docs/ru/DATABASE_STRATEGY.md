# Стратегия базы данных и хранилища файлов

## Проблема

Нужно найти бесплатное, безопасное и надежное решение для:
- PostgreSQL базы данных
- Хранилища файлов (студенты прикрепляют много файлов)
- Работает постоянно (даже при выключенном ПК)
- Без зависимости от платных сервисов

## ⚠️ Важное понимание

**Локальная база данных НЕ будет работать при выключенном ПК!**

Если вы установите PostgreSQL на свой компьютер:
- ✅ Работает локально для разработки
- ❌ Недоступна при выключенном ПК
- ❌ Недоступна из интернета (студенты не смогут зайти)
- ❌ Нужен постоянный доступ к вашему ПК

**Вывод:** Для production нужен облачный хостинг.

---

## 🆓 Бесплатные варианты для PostgreSQL

### 1. **Supabase (Рекомендуется) ⭐**

**Плюсы:**
- ✅ Полностью бесплатный тариф (500MB БД, 1GB файлов)
- ✅ PostgreSQL с расширенными функциями
- ✅ Встроенное хранилище файлов (Storage)
- ✅ Аутентификация из коробки
- ✅ Row Level Security (RLS) для безопасности
- ✅ Автоматические бэкапы
- ✅ API из коробки
- ✅ Реальное время (realtime subscriptions)

**Ограничения бесплатного тарифа:**
- 500MB базы данных
- 1GB файлового хранилища
- 2GB трафика в месяц
- 50,000 запросов в месяц

**Для вашего проекта:**
- Если студентов ~1000-2000, этого хватит
- Можно оптимизировать (сжатие файлов, удаление старых)
- При росте можно перейти на платный тариф ($25/мес)

**Безопасность:**
- ✅ Шифрование данных
- ✅ RLS политики
- ✅ HTTPS по умолчанию
- ✅ Аудит доступа

**Настройка:**
```bash
# Уже настроено в проекте!
# Просто создайте проект на supabase.com
# И добавьте ключи в .env.local
```

---

### 2. **Neon (Альтернатива Supabase)**

**Плюсы:**
- ✅ Бесплатный тариф (3GB БД)
- ✅ Serverless PostgreSQL
- ✅ Автоматическое масштабирование
- ✅ Автоматические бэкапы

**Минусы:**
- ❌ Нет встроенного файлового хранилища (нужен отдельный сервис)
- ❌ Нет аутентификации из коробки

**Ссылка:** https://neon.tech

---

### 3. **Railway (Простой вариант)**

**Плюсы:**
- ✅ $5 бесплатных кредитов в месяц
- ✅ Простая настройка
- ✅ Автоматический деплой

**Минусы:**
- ❌ Ограниченные бесплатные кредиты
- ❌ Может закончиться в середине месяца

**Ссылка:** https://railway.app

---

### 4. **Render (Стабильный вариант)**

**Плюсы:**
- ✅ Бесплатный PostgreSQL (90 дней, потом $7/мес)
- ✅ Надежная инфраструктура

**Минусы:**
- ❌ После 90 дней становится платным
- ❌ Нет файлового хранилища

**Ссылка:** https://render.com

---

## 📁 Бесплатные варианты для файлового хранилища

### 1. **Supabase Storage (Рекомендуется) ⭐**

**Плюсы:**
- ✅ Встроено в Supabase
- ✅ 1GB бесплатно
- ✅ Политики доступа (RLS)
- ✅ CDN для быстрой загрузки
- ✅ Автоматическое сжатие изображений

**Использование:**
```typescript
// Загрузка файла
const { data, error } = await supabase.storage
  .from('appeals')
  .upload(`${appealId}/${fileName}`, file);

// Получение публичной ссылки
const { data } = supabase.storage
  .from('appeals')
  .getPublicUrl(`${appealId}/${fileName}`);
```

---

### 2. **Cloudflare R2 (Бесплатно 10GB!)**

**Плюсы:**
- ✅ 10GB бесплатно
- ✅ Неограниченные запросы
- ✅ S3-совместимый API
- ✅ CDN встроен

**Минусы:**
- ❌ Нужна отдельная настройка
- ❌ Нет встроенной интеграции с Supabase

**Ссылка:** https://www.cloudflare.com/products/r2/

**Настройка:**
```typescript
// Нужно установить @aws-sdk/client-s3
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';

const s3Client = new S3Client({
  region: 'auto',
  endpoint: `https://${accountId}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: process.env.R2_ACCESS_KEY_ID!,
    secretAccessKey: process.env.R2_SECRET_ACCESS_KEY!,
  },
});
```

---

### 3. **Backblaze B2 (5GB бесплатно)**

**Плюсы:**
- ✅ 5GB бесплатно
- ✅ S3-совместимый
- ✅ Надежная инфраструктура

**Минусы:**
- ❌ Меньше бесплатного места, чем R2

**Ссылка:** https://www.backblaze.com/b2/cloud-storage.html

---

### 4. **GitHub LFS (Для небольших файлов)**

**Плюсы:**
- ✅ 1GB бесплатно
- ✅ Интеграция с Git

**Минусы:**
- ❌ Не предназначен для production файлов
- ❌ Ограничения по размеру файла

---

## 🎯 Рекомендуемая архитектура

### Вариант 1: Supabase (Все в одном) ⭐ РЕКОМЕНДУЕТСЯ

```
┌─────────────────────────────────┐
│      Supabase (Бесплатно)       │
├─────────────────────────────────┤
│  PostgreSQL (500MB)            │
│  Storage (1GB файлов)           │
│  Auth (встроено)                │
│  API (автоматически)             │
└─────────────────────────────────┘
```

**Плюсы:**
- ✅ Все в одном месте
- ✅ Простая настройка
- ✅ Уже настроено в проекте
- ✅ Безопасность из коробки

**Минусы:**
- ⚠️ 1GB может быть мало при большом количестве файлов

**Решение:** Оптимизация файлов
- Сжатие изображений перед загрузкой
- Удаление старых файлов (автоматическая очистка)
- Использование внешних ссылок для больших файлов

---

### Вариант 2: Гибридный (Supabase + Cloudflare R2)

```
┌─────────────────┐      ┌──────────────────┐
│   Supabase      │      │  Cloudflare R2   │
│   PostgreSQL    │      │  10GB файлов      │
│   Auth          │      │  CDN              │
└─────────────────┘      └──────────────────┘
```

**Плюсы:**
- ✅ Больше места для файлов (10GB)
- ✅ Быстрая загрузка через CDN
- ✅ Бесплатно

**Минусы:**
- ❌ Сложнее настройка
- ❌ Два сервиса вместо одного

---

## 💾 Локальная разработка (DataGrip)

Для локальной разработки можно использовать локальный PostgreSQL:

### Установка PostgreSQL

**Windows:**
1. Скачайте с https://www.postgresql.org/download/windows/
2. Установите (запомните пароль для пользователя `postgres`)
3. Запустите pgAdmin или используйте DataGrip

**Настройка в DataGrip:**
1. File → New → Data Source → PostgreSQL
2. Host: `localhost`
3. Port: `5432`
4. Database: `oss_dvfu`
5. User: `postgres`
6. Password: (ваш пароль)

### Создание базы данных

```sql
-- В DataGrip или psql
CREATE DATABASE oss_dvfu;
```

### Применение схемы

```sql
-- Выполните в DataGrip:
-- 1. database/schema.sql
-- 2. database/seed.sql (опционально)
```

### Переключение между локальной и облачной БД

**`.env.local` (локальная разработка):**
```env
# Локальная PostgreSQL (через Supabase локально или прямой доступ)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-local-key

# Или прямой доступ к PostgreSQL (если настроен Supabase локально)
DATABASE_URL=postgresql://postgres:password@localhost:5432/oss_dvfu
```

**`.env.production` (облачная):**
```env
# Supabase облачный
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

---

## 🔒 Безопасность

### 1. Row Level Security (RLS)

Supabase использует RLS для безопасности:

```sql
-- Пример: студенты видят только свои обращения
CREATE POLICY "Users can view own appeals"
ON appeals FOR SELECT
USING (auth.uid() = user_id);
```

### 2. Политики доступа к файлам

```sql
-- Файлы доступны только владельцу
CREATE POLICY "Users can upload own files"
ON storage.objects FOR INSERT
WITH CHECK (bucket_id = 'appeals' AND auth.uid()::text = (storage.foldername(name))[1]);
```

### 3. Ограничение размера файлов

```typescript
// В коде загрузки
const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
if (file.size > MAX_FILE_SIZE) {
  throw new Error('Файл слишком большой');
}
```

### 4. Валидация типов файлов

```typescript
const ALLOWED_TYPES = ['image/jpeg', 'image/png', 'application/pdf'];
if (!ALLOWED_TYPES.includes(file.type)) {
  throw new Error('Неподдерживаемый тип файла');
}
```

---

## 📊 Оптимизация использования места

### 1. Сжатие изображений

```typescript
// Используйте библиотеку для сжатия
import imageCompression from 'browser-image-compression';

const compressedFile = await imageCompression(file, {
  maxSizeMB: 1,
  maxWidthOrHeight: 1920,
});
```

### 2. Автоматическая очистка старых файлов

```sql
-- Удаление файлов обращений старше 1 года
DELETE FROM storage.objects
WHERE bucket_id = 'appeals'
  AND created_at < NOW() - INTERVAL '1 year';
```

### 3. Использование внешних ссылок

Для больших файлов (видео, архивы) можно хранить только ссылку:

```sql
-- В таблице appeals добавить поле
ALTER TABLE appeals ADD COLUMN external_file_url TEXT;
```

---

## 🚀 План миграции

### Этап 1: Текущее состояние (Supabase)
- ✅ Уже настроено
- ✅ Работает
- ✅ Бесплатно

### Этап 2: Оптимизация (если нужно больше места)
- Добавить сжатие файлов
- Настроить автоматическую очистку
- Мониторинг использования

### Этап 3: Масштабирование (при росте)
- Переход на платный тариф Supabase ($25/мес)
- Или миграция на Cloudflare R2 для файлов

---

## 📝 Чек-лист настройки

### Для локальной разработки:
- [ ] Установить PostgreSQL
- [ ] Настроить DataGrip
- [ ] Создать базу данных `oss_dvfu`
- [ ] Выполнить `database/schema.sql`
- [ ] Выполнить `database/seed.sql` (опционально)
- [ ] Настроить `.env.local`

### Для production:
- [ ] Создать проект на Supabase
- [ ] Выполнить `database/schema.sql` в Supabase SQL Editor
- [ ] Настроить переменные окружения в Vercel
- [ ] Настроить RLS политики
- [ ] Настроить Storage buckets
- [ ] Протестировать загрузку файлов

---

## ❓ FAQ

**Q: Можно ли использовать только локальную БД?**
A: Нет, для production нужен облачный хостинг. Локальная БД только для разработки.

**Q: Что делать, когда закончится бесплатное место?**
A: 
1. Оптимизировать файлы (сжатие)
2. Удалить старые файлы
3. Перейти на платный тариф или добавить Cloudflare R2

**Q: Безопасно ли хранить файлы студентов в облаке?**
A: Да, если правильно настроены политики доступа (RLS) и шифрование.

**Q: Можно ли использовать несколько бесплатных аккаунтов?**
A: Технически да, но это нарушает условия использования. Лучше оптимизировать.

---

## 🎯 Итоговая рекомендация

**Используйте Supabase:**
- ✅ Уже настроено в проекте
- ✅ Бесплатно для начала
- ✅ Все в одном месте
- ✅ Безопасно
- ✅ Легко масштабировать

**Для файлов:**
- Начните с Supabase Storage (1GB)
- При необходимости добавьте Cloudflare R2 (10GB бесплатно)

**Для разработки:**
- Используйте локальный PostgreSQL через DataGrip
- Переключайтесь на Supabase для тестирования production-окружения

