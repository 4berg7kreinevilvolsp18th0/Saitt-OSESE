# Настройка GitHub Actions для автоматической синхронизации с Supabase

## Обзор

GitHub Actions позволяет автоматически применять миграции базы данных к Supabase при каждом коммите в репозиторий.

## Преимущества

- ✅ Автоматическое применение миграций
- ✅ Синхронизация между разработчиками
- ✅ История изменений в Git
- ✅ Откат изменений через Git
- ✅ Проверка миграций перед применением

---

## Шаг 1: Установка Supabase CLI локально (опционально)

**⚠️ ВАЖНО:** 
- Supabase CLI нельзя устанавливать через `npm install -g`!
- Если Scoop выдает ошибку распаковки, используйте npx (Вариант C)

### Вариант A: Через Scoop (может иметь проблемы)

1. Установите Scoop (если еще не установлен):
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
Invoke-RestMethod -Uri https://get.scoop.sh | Invoke-Expression
```

2. Установите Supabase CLI:
```powershell
scoop bucket add supabase https://github.com/supabase/scoop-bucket.git
scoop install supabase
```

3. Проверка:
```powershell
supabase --version
```

### Вариант B: Через Chocolatey

1. Установите Chocolatey (если еще не установлен):
```powershell
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
```

2. Установите Supabase CLI:
```powershell
choco install supabase
```

3. Проверка:
```powershell


```

### Вариант C: Использование npx (РЕКОМЕНДУЕТСЯ) ⭐

**Самый простой способ!** Не требует установки менеджеров пакетов.

```powershell
# Использование без установки
npx supabase@latest login
npx supabase@latest link --project-ref your-project-id
npx supabase@latest db push
```

**Для удобства создайте алиас:**
```powershell
# Добавьте в PowerShell профиль ($PROFILE)
function supabase { npx supabase@latest $args }
```

Теперь можно использовать просто: `supabase --version`

### Вариант D: Ручная установка

1. Скачайте **ZIP** (не tar.gz!) с https://github.com/supabase/cli/releases
2. Распакуйте `supabase.exe` в папку (например, `C:\Tools\supabase`)
3. Добавьте в PATH:
   ```powershell
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\Tools\supabase", "User")
   ```

**Примечание:** Для GitHub Actions установка не требуется - CLI устанавливается автоматически в workflow.

---

## Шаг 2: Получение токенов и ID

### 2.1: Access Token

1. Перейдите в Supabase Dashboard
2. Откройте **Settings** → **Access Tokens**
3. Нажмите **"Generate new token"**
4. Дайте имя: `GitHub Actions`
5. Скопируйте токен (показывается только один раз!)

### 2.2: Project ID

1. В Supabase Dashboard откройте **Settings** → **General**
2. Найдите **Reference ID** (выглядит как `abcdefghijklmnop`)
3. Скопируйте его

### 2.3: Database Password

Используйте пароль, который вы создали при создании проекта Supabase.

---

## Шаг 3: Настройка GitHub Secrets

1. В вашем GitHub репозитории перейдите в **Settings**
2. Откройте **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**

### Добавьте следующие секреты:

| Name | Value | Описание |
|------|-------|----------|
| `SUPABASE_ACCESS_TOKEN` | `sbp_...` | Access Token из Supabase |
| `SUPABASE_PROJECT_ID` | `abcdefghijklmnop` | Reference ID проекта |
| `SUPABASE_DB_PASSWORD` | `YourPassword123!` | Пароль базы данных |

**Важно:** 
- Не делитесь этими секретами
- Не коммитьте их в репозиторий
- Используйте только через GitHub Secrets

---

## Шаг 4: Создание workflow файла

Workflow файл уже создан: `.github/workflows/supabase-sync.yml`

Он автоматически:
- Запускается при изменении файлов в `database/`
- Применяет миграции к Supabase
- Проверяет статус миграций

---

## Шаг 5: Тестирование

### 5.1: Тест локально

```bash
# Войдите в Supabase
supabase login

# Свяжите проект
supabase link --project-ref your-project-id

# Проверьте миграции
supabase migration list

# Примените миграции
supabase db push
```

### 5.2: Тест через GitHub Actions

1. Сделайте небольшое изменение в файле миграции (например, добавьте комментарий)
2. Закоммитьте и запушьте:
```bash
git add database/migrations/
git commit -m "Test: проверка GitHub Actions"
git push
```
3. Перейдите в GitHub → **Actions**
4. Убедитесь, что workflow запустился
5. Проверьте, что он выполнился успешно (зеленая галочка)

---

## Шаг 6: Ручной запуск workflow

Если нужно запустить workflow вручную:

1. Перейдите в GitHub → **Actions**
2. Выберите workflow **"Supabase Database Sync"**
3. Нажмите **"Run workflow"**
4. Выберите ветку (обычно `main`)
5. Нажмите **"Run workflow"**

---

## Структура миграций

Миграции должны находиться в папке `database/migrations/`:

```
database/
  migrations/
    add_file_retention.sql
    improve_anonymity.sql
    add_content_protection.sql
    fix_security_issues.sql
    ...
```

**Правила именования:**
- Используйте описательные имена
- Формат: `описание_изменения.sql`
- Примеры:
  - `add_user_table.sql`
  - `fix_rls_policies.sql`
  - `add_indexes.sql`

---

## Откат миграций

Если нужно откатить миграцию:

### Вариант 1: Через Supabase Dashboard

1. Перейдите в **Database** → **Migrations**
2. Найдите нужную миграцию
3. Создайте новую миграцию для отката

### Вариант 2: Через SQL

1. Создайте файл `database/migrations/rollback_xxx.sql`
2. Добавьте SQL для отката изменений
3. Закоммитьте и запушьте

---

## Мониторинг

### Проверка статуса миграций

В Supabase Dashboard:
1. Перейдите в **Database** → **Migrations**
2. Увидите список всех примененных миграций
3. Можно увидеть, когда каждая миграция была применена

### Логи GitHub Actions

1. Перейдите в GitHub → **Actions**
2. Выберите последний запуск
3. Откройте job **"sync-database"**
4. Просмотрите логи каждого шага

---

## Решение проблем

### Проблема: "Authentication failed"

**Решение:**
1. Проверьте, что `SUPABASE_ACCESS_TOKEN` правильный
2. Убедитесь, что токен не истек
3. Создайте новый токен, если нужно

### Проблема: "Project not found"

**Решение:**
1. Проверьте, что `SUPABASE_PROJECT_ID` правильный
2. Убедитесь, что проект существует и активен

### Проблема: "Migration failed"

**Решение:**
1. Проверьте логи в GitHub Actions
2. Проверьте SQL синтаксис миграции
3. Убедитесь, что миграция не конфликтует с существующими данными

### Проблема: "Workflow не запускается"

**Решение:**
1. Проверьте, что файл `.github/workflows/supabase-sync.yml` существует
2. Убедитесь, что файлы в `database/` изменены
3. Проверьте, что коммит запушен в ветку `main` или `master`

---

## Безопасность

### ✅ Хорошие практики

- Используйте GitHub Secrets для токенов
- Не коммитьте пароли и токены
- Ограничьте доступ к репозиторию
- Регулярно обновляйте токены
- Используйте разные токены для разных окружений

### ❌ Чего не делать

- Не храните секреты в коде
- Не делитесь токенами
- Не используйте один токен для всех проектов
- Не коммитьте `.env` файлы

---

## Дополнительные настройки

### Уведомления

Можно добавить уведомления в Slack, Discord или email при успехе/ошибке:

```yaml
- name: Notify Slack on failure
  if: failure()
  uses: 8398a7/action-slack@v3
  with:
    status: ${{ job.status }}
    text: 'Миграция Supabase не удалась'
  env:
    SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
```

### Расписание

Workflow уже настроен на запуск каждую ночь в 3:00 UTC для проверки синхронизации.

Можно изменить в `.github/workflows/supabase-sync.yml`:

```yaml
schedule:
  - cron: '0 3 * * *'  # Каждый день в 3:00 UTC
```

---

## Готово! 🎉

Теперь при каждом изменении файлов в `database/` миграции будут автоматически применяться к Supabase.

Для проверки:
1. Сделайте тестовое изменение
2. Закоммитьте и запушьте
3. Проверьте GitHub Actions
4. Убедитесь, что миграция применена в Supabase

