# 🔧 Исправление ошибки 404 на Vercel

## Проблема

После деплоя на Vercel появляется ошибка **404: NOT_FOUND**.

## Причины и решения

### ✅ Исправлено в коде:

1. **Убрали `output: 'standalone'` из `next.config.js`**
   - Vercel лучше работает с обычным режимом Next.js
   - Standalone режим может вызывать проблемы с роутингом

2. **Упростили `vercel.json`**
   - Убрали конфликтующие настройки
   - Vercel автоматически определяет Next.js проект

3. **Переписали `app/content/[slug]/page.tsx`**
   - Убрали использование `fs` (не работает на Vercel)
   - Теперь использует Supabase для получения контента

## Что нужно сделать в Vercel:

### 1. Проверьте настройки проекта

Зайдите в **Settings → General** и убедитесь:

```
Framework Preset: Next.js ✅
Root Directory: frontend/nextjs ⚠️ ОБЯЗАТЕЛЬНО!
Build Command: npm run build (или по умолчанию)
Output Directory: .next (или по умолчанию)
Install Command: npm install (или по умолчанию)
```

### 2. Проверьте Environment Variables

**Settings → Environment Variables** должны быть:

```
NEXT_PUBLIC_SUPABASE_URL = ваш-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY = ваш-supabase-anon-key
NEXT_PUBLIC_SITE_URL = https://ваш-проект.vercel.app
```

### 3. Перезапустите деплой

1. Зайдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"Redeploy"** (три точки → Redeploy)

Или сделайте новый push в GitHub:

```bash
git add .
git commit -m "Fix 404 error: remove standalone mode, fix content page"
git push origin main
```

## Проверка после исправления

1. ✅ Главная страница (`/`) должна загружаться
2. ✅ Страница контента (`/content`) должна работать
3. ✅ Отдельные статьи (`/content/[slug]`) должны открываться
4. ✅ Форма обращений (`/appeal`) должна работать

## Если ошибка осталась

### Проверьте логи сборки:

1. Зайдите в **Deployments**
2. Откройте последний деплой
3. Посмотрите **Build Logs**
4. Найдите ошибки (обычно красным цветом)

### Частые проблемы:

**Ошибка "Module not found":**
- Проверьте, что `package.json` находится в `frontend/nextjs/`
- Убедитесь, что все зависимости установлены

**Ошибка "Cannot find module":**
- Проверьте импорты в коде
- Убедитесь, что все файлы на месте

**Ошибка "Environment variable not found":**
- Проверьте Environment Variables в Vercel
- Убедитесь, что переменные добавлены для Production, Preview, Development

## Дополнительная помощь

Если проблема не решена:
1. Проверьте логи в Vercel Dashboard
2. Проверьте консоль браузера (F12)
3. Убедитесь, что Supabase проект настроен правильно
4. Проверьте, что база данных содержит данные

