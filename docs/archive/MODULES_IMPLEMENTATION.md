# Реализованные модули

## ✅ Завершено

### 1. Вложения к обращениям
- ✅ Таблица `appeal_attachments` в базе данных
- ✅ Компонент `FileUpload` для загрузки файлов
- ✅ API endpoint `/api/upload` для загрузки в Supabase Storage
- ✅ Интеграция в форму обращения
- ✅ Поддержка до 5 файлов, максимум 10MB каждый

**Настройка:** См. `docs/STORAGE_SETUP.md`

### 2. Улучшенные статусы обращений
- ✅ Компонент `AppealStatusBadge` с понятными названиями
- ✅ Утилиты в `lib/appealStatus.ts`
- ✅ Обновлена страница проверки статуса
- ✅ Статусы теперь показывают:
  - "Принято" вместо "new"
  - "В работе" вместо "in_progress"
  - "Нужна информация" вместо "waiting"
  - "Решено" вместо "closed"
- ✅ Добавлены описания статусов для студентов

### 3. Назначение ответственных и дедлайны
- ✅ Поле `assigned_to` в таблице `appeals`
- ✅ Поле `deadline` в таблице `appeals`
- ✅ Компонент `AppealCard` с возможностью:
  - Назначить ответственного
  - Установить приоритет (низкий/обычный/высокий/срочный)
  - Установить дедлайн
- ✅ Визуальное выделение просроченных обращений

### 4. Приоритеты обращений
- ✅ Поле `priority` в таблице `appeals`
- ✅ Цветовая индикация приоритетов
- ✅ Фильтрация и сортировка по приоритету

### 5. Экспорт отчётов
- ✅ API endpoint `/api/export/appeals` для экспорта в CSV
- ✅ Кнопка экспорта в админ-панели
- ✅ Экспорт включает все поля обращений
- ✅ Поддержка кириллицы в CSV (BOM)

### 6. Telegram уведомления (базовая реализация)
- ✅ API endpoint `/api/notifications/telegram`
- ✅ Автоматическая отправка при изменении статуса
- ✅ Отправка только для Telegram контактов
- ⚠️ Требует настройки Telegram Bot API

## 🔄 В процессе

### 7. Шаблоны ответов
- ⏳ Таблица `response_templates` (нужно создать)
- ⏳ Компонент выбора шаблона
- ⏳ Интеграция в админ-панель

## 📋 Планируется

### 8. Теги для обращений
- Поле `tags` (массив) уже добавлено в схему
- Нужно добавить UI для управления тегами

### 9. Расширенная аналитика
- SLA метрики
- Время до первого ответа
- Просроченные обращения
- Нагрузка по ответственным

## Инструкции по настройке

### Supabase Storage для вложений

1. Создайте bucket `appeal-attachments` в Supabase Storage
2. Настройте политики доступа (см. `docs/STORAGE_SETUP.md`)
3. Убедитесь, что bucket публичный

### Telegram уведомления

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Добавьте токен в Vercel: `TELEGRAM_BOT_TOKEN`
3. (Опционально) Настройте канал для уведомлений: `TELEGRAM_NOTIFICATION_CHANNEL`

**Ограничение:** Telegram Bot API не позволяет отправлять сообщения пользователям напрямую. Нужно либо:
- Пользователь должен начать диалог с ботом
- Использовать канал для публикации уведомлений
- Использовать webhook/polling для получения сообщений

### Обновление базы данных

Выполните обновленный `database/schema.sql` в Supabase SQL Editor для добавления новых полей:
- `assigned_to` в таблице `appeals`
- `priority` в таблице `appeals`
- `tags` в таблице `appeals`
- Таблица `appeal_attachments`

## Использование

### Загрузка файлов
1. При подаче обращения пользователь может прикрепить до 5 файлов
2. Файлы автоматически загружаются в Supabase Storage
3. Информация о файлах сохраняется в `appeal_attachments`

### Назначение ответственных
1. В админ-панели `/admin/appeals` нажмите "Ответственный" на карточке обращения
2. Выберите пользователя из списка
3. Обращение будет назначено выбранному пользователю

### Установка приоритета
1. Нажмите "Приоритет" на карточке обращения
2. Выберите: Низкий, Обычный, Высокий, Срочный
3. Просроченные обращения с дедлайном выделяются красным

### Экспорт данных
1. В админ-панели нажмите "Экспорт CSV"
2. Файл загрузится автоматически
3. Можно открыть в Excel или другой программе

