# 🎯 Настройка проекта в Vercel

## Что выбрать в Vercel

### При создании нового проекта:

1. **Нажмите "Add New..." → "Project"** ✅
   - Это правильный выбор для вашего сайта
   - Project - это основной тип для веб-приложений

2. **НЕ выбирайте:**
   - ❌ Domain - это для добавления домена к существующему проекту
   - ❌ Store - это для интернет-магазинов
   - ❌ Integration - это для интеграций с другими сервисами
   - ❌ Team Member - это для добавления участников команды

## Пошаговая настройка проекта

### Шаг 1: Импорт репозитория

1. После выбора "Project" вы увидите список ваших GitHub репозиториев
2. Найдите ваш репозиторий `oss-dvfu-site` (или как вы его назвали)
3. Нажмите **"Import"**

### Шаг 2: Настройка проекта

**ВАЖНО:** Укажите правильные параметры!

```
Project Name: oss-dvfu-site (или любое другое название)
⚠️ Правила для Project Name:
   - Только буквы (a-z, A-Z), цифры (0-9) и подчеркивания (_)
   - НЕ может начинаться с цифры
   - НЕ используйте дефисы (-), пробелы, спецсимволы
   - Примеры правильных имен: oss_dvfu_site, ossDvfuSite, ossdvfu
   - Примеры НЕПРАВИЛЬНЫХ: oss-dvfu-site, 123project, my site

Framework Preset: Next.js ✅
Root Directory: frontend/nextjs ⚠️ ОБЯЗАТЕЛЬНО!
Build Command: npm run build (или оставьте по умолчанию)
Output Directory: .next (или оставьте по умолчанию)
Install Command: npm install (или оставьте по умолчанию)
```

### Шаг 3: Environment Variables

Добавьте переменные окружения:

1. Нажмите **"Environment Variables"**
2. Добавьте каждую переменную:

```
NEXT_PUBLIC_SUPABASE_URL
= ваш-supabase-project-url
(например: https://xxxxx.supabase.co)

NEXT_PUBLIC_SUPABASE_ANON_KEY
= ваш-supabase-anon-key
(найти в Supabase Dashboard → Settings → API)

NEXT_PUBLIC_SITE_URL
= https://ваш-проект.vercel.app
(можно обновить после первого деплоя)

### Шаг 4: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 2-3 минуты)
3. После успешного деплоя вы получите URL вашего сайта

### Шаг 5: Обновление URL (после деплоя)

1. Скопируйте URL из Vercel (например: `https://oss-dvfu-site.vercel.app`)
2. Зайдите в Settings → Environment Variables
3. Обновите `NEXT_PUBLIC_SITE_URL` на ваш реальный URL
4. Нажмите **"Redeploy"** для применения изменений

## Настройки после деплоя

### Production Branch

1. Settings → Git
2. **Production Branch:** `main` (или `master`)
3. Это ветка, которая будет деплоиться на production

### Preview Deployments

1. Settings → Git
2. Включите **"Automatic Preview Deployments"**
3. Теперь каждый Pull Request получит свой preview URL

### Custom Domain (опционально)

1. Settings → Domains
2. Добавьте ваш домен (например: `oss-dvfu.ru`)
3. Следуйте инструкциям по настройке DNS

## Рекомендуемые настройки

### Build & Development Settings:

```
Framework: Next.js
Build Command: npm run build
Output Directory: .next
Install Command: npm install
Development Command: npm run dev
```

### Git Settings:

```
Production Branch: main
Preview Deployments: Enabled ✅
Automatic Deployments: Enabled ✅
```

### Environment Variables:

Всегда добавляйте в **Production, Preview, Development**:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `NEXT_PUBLIC_SITE_URL`

## Проверка после деплоя

1. ✅ Откройте ваш сайт по URL из Vercel
2. ✅ Проверьте, что главная страница загружается
3. ✅ Проверьте, что формы работают
4. ✅ Проверьте консоль браузера на ошибки

## Troubleshooting

### Ошибка "The name contains invalid characters":
**Эта ошибка возникает при неправильном Project Name!**

✅ **Правильные имена:**
- `oss_dvfu_site` (используйте подчеркивания вместо дефисов)
- `ossDvfuSite` (camelCase)
- `ossdvfu` (все маленькие буквы)

❌ **Неправильные имена:**
- `oss-dvfu-site` (дефисы недопустимы)
- `123project` (не может начинаться с цифры)
- `my site` (пробелы недопустимы)
- `project.name` (точки недопустимы)

**Решение:**
1. Измените Project Name на имя без дефисов (используйте подчеркивания `_`)
2. Убедитесь, что имя не начинается с цифры
3. Используйте только буквы, цифры и подчеркивания

### Ошибка "Build Failed":
- Проверьте, что Root Directory указан правильно: `frontend/nextjs`
- Убедитесь, что все Environment Variables добавлены
- Проверьте логи сборки в Vercel Dashboard

### Ошибка "Module not found":
- Убедитесь, что `package.json` находится в `frontend/nextjs/`
- Проверьте, что все зависимости указаны в `package.json`

### Сайт не работает после деплоя:
- Проверьте Environment Variables
- Убедитесь, что Supabase проект настроен правильно
- Проверьте логи в Vercel Dashboard → Deployments

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js on Vercel](https://vercel.com/docs/frameworks/nextjs)
- [Environment Variables](https://vercel.com/docs/projects/environment-variables)


