# 🚀 Автоматический деплой на GitHub

Проект настроен для автоматического деплоя при каждом пуше в репозиторий.

## Как это работает

### При пуше в ветку `main` или `master`:
1. ✅ Автоматически запускается CI (проверка кода)
2. ✅ Проект собирается
3. ✅ Автоматически деплоится на Vercel

### При создании Pull Request:
1. ✅ Запускается CI для проверки
2. ✅ Создаётся preview деплой (если настроено)

## Настройка (один раз)

### Шаг 1: Подключите Vercel к GitHub

1. Зайдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите **"Add New Project"**
3. Выберите ваш GitHub репозиторий
4. Настройте:
   - **Root Directory:** `frontend/nextjs`
   - **Framework:** Next.js
   - **Build Command:** `npm run build`
5. Добавьте Environment Variables
6. Нажмите **"Deploy"**

После этого Vercel автоматически будет деплоить при каждом пуше!

### Шаг 2: Настройте GitHub Actions (опционально)

Если хотите дополнительный контроль через GitHub Actions:

1. **Получите Vercel токены:**
   - Vercel Dashboard → Settings → Tokens
   - Создайте токен

2. **Добавьте Secrets в GitHub:**
   - Репозиторий → Settings → Secrets and variables → Actions
   - Добавьте:
     ```
     VERCEL_TOKEN
     VERCEL_ORG_ID
     VERCEL_PROJECT_ID
     NEXT_PUBLIC_SUPABASE_URL
     NEXT_PUBLIC_SUPABASE_ANON_KEY
     NEXT_PUBLIC_SITE_URL
     ```

3. **Workflow файлы уже созданы:**
   - `.github/workflows/deploy.yml` - деплой
   - `.github/workflows/ci.yml` - проверка сборки

## Использование

### Обычный workflow:

```bash
# 1. Создайте ветку для изменений
git checkout -b feature/new-feature

# 2. Внесите изменения
# ... редактируйте файлы ...

# 3. Закоммитьте изменения
git add .
git commit -m "Add new feature"

# 4. Запушьте в GitHub
git push origin feature/new-feature

# 5. Создайте Pull Request на GitHub
# GitHub автоматически запустит CI

# 6. После мерджа в main - автоматический деплой!
```

### Проверка статуса деплоя:

1. **GitHub:**
   - Зайдите в репозиторий → Actions
   - Видите все запуски workflow

2. **Vercel:**
   - Dashboard → Deployments
   - Видите все деплои и их статус

## Что деплоится автоматически

- ✅ Каждый коммит в `main`/`master`
- ✅ Каждый Pull Request (preview)
- ✅ После мерджа Pull Request

## Что НЕ деплоится автоматически

- ❌ Коммиты в другие ветки (только CI проверка)
- ❌ Draft Pull Requests (если настроено)

## Мониторинг

### GitHub Actions:
- Репозиторий → **Actions** вкладка
- Видите статус всех workflow
- Логи каждого шага

### Vercel:
- Dashboard → **Deployments**
- Статус каждого деплоя
- Логи сборки
- Preview URLs для PR

## Troubleshooting

### Деплой не запускается:
1. Проверьте, что Vercel подключен к репозиторию
2. Убедитесь, что вы пушите в правильную ветку (`main`)
3. Проверьте настройки в Vercel Dashboard

### Ошибка сборки:
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что Root Directory указан правильно

### GitHub Actions не работает:
1. Проверьте, что secrets добавлены
2. Убедитесь, что workflow файлы в `.github/workflows/`
3. Проверьте логи в GitHub Actions

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Подробная настройка](./GITHUB_ACTIONS_SETUP.md)

